import 'package:flutter/material.dart';

class LoginORegister extends StatelessWidget {
  const LoginORegister({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}